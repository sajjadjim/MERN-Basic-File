import React, { use } from 'react';

const Users2 = ({useData}) => {
    const userData2 = use(useData)
    console.log(userData2)
    return (
        <div>
            
        </div>
    );
};

export default Users2;